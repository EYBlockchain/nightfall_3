
from "./common/utils/structures.zok" import Point, PublicTransaction;
from "./common/generic_circuit/Generics/deposit_generic.zok" import deposit_generic;

def main(\
    PublicTransaction<0,1> tx,\
    field[0] roots,\
    field maticAddress,\
    private field salt,\
    private Point recipientPublicKey\
) {
    assert(deposit_generic::<0,1>(\
        tx.value,\
        tx.fee,\
	    tx.transactionType,\
	    tx.tokenType,\
        tx.historicRootBlockNumberL2,\
	    tx.tokenId,\
	    tx.ercAddress,\
	    tx.recipientAddress,\
	    tx.commitments,\
	    tx.nullifiers,\
	    tx.compressedSecrets,\
        roots,\
        maticAddress,\
        salt,\
        recipientPublicKey\
        ));
}