#!/bin/bash

echo "Setting up the environment variables for nf3 deployer ..."

source bin/basesepolia-deployment.env <private key without 0x> <https RPC provider>

echo "Running nightfall deployer script..."

./bin/deploy-contracts .env.deployment.basesepolia