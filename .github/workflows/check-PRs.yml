name: Check PR

on:
  push:
    branch:
      - liju.jose/fix-github-action-on-westlad/priority-queues

jobs:
  eslint-check:
    runs-on: ubuntu-18.04
    steps:
      - uses: actions/checkout@master
      - uses: actions/setup-node@v1
        with:
          node-version: '14.17.0'
      - name: eslint check
        run: |
          npm ci
          npm run lint

  ganache-test:
    runs-on: ubuntu-18.04
    steps:
      - uses: actions/checkout@master
      - uses: actions/setup-node@v1
        with:
          node-version: '14.17.0'

      - name: Start Containers
        run: |
          docker-compose build
          ./start-nightfall -g &> ganache-test.log &disown

      - name: Run integration test
        run: |
          npm ci
          sleep 200
          npm test

      - name: If integration test failed, shutdown the Containers
        if: failure()
        run: docker-compose -f docker-compose.yml -f docker-compose.ganache.yml down -v

      - name: If integration test failed, upload logs files as artifacts
        if: failure()
        uses: actions/upload-artifact@master
        with:
          name: ganache-test-logs
          path: ./ganache-test.log

  test-gas:
    name: check gas for 32 transactions per block
    runs-on: ubuntu-20.04
    steps:
      - run: df -h
      - uses: actions/checkout@master
      - run: df -h
      - uses: actions/setup-node@v1
        with:
          node-version: '14.17.0'

      - run: df -h
      - name: Start Containers with ganache
        run: |
          docker-compose build
          ./start-nightfall -g &> test-gas.log &disown
        env:
          TRANSACTIONS_PER_BLOCK: 32

      - run: df -h
      - run: swapon -s
      - run: free -h
      - name: Run tx-gas.mjs test suites
        run: |
          npm ci
          sleep 200
          npm run test-gas

      - run: df -h
      - run: swapon -s
      - run: free -h
      - name: If integration test failed, shutdown the Containers
        if: failure()
        run: |
          df -h
          swapon -s
          free -h
          docker-compose -f docker-compose.yml -f docker-compose.ganache.yml down -v

      - name: If integration test failed, upload logs files as artifacts
        if: failure()
        uses: actions/upload-artifact@master
        with:
          name: test-gas-logs
          path: ./test-gas.log
