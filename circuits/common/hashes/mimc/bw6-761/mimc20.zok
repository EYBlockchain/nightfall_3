// MiMC hashing function for twenty input fields

from "./mimc-encryption.zok" import main as mimcp

def main(field[20] a)->(field):
  field r = 0
  for field i in 0..20 do
    r = r + a[i] + mimcp(a[i], r)
  endfor
  return r
