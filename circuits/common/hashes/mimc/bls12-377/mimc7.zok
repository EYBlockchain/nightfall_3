// MiMC hashing function for seven input fields

from "./mimc-encryption.zok" import main as mimcp

def main(field[7] a)->(field):
  field r = 0
  for field i in 0..7 do
    r = r + a[i] + mimcp(a[i], r)
  endfor
  return r
